<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base LearnHub app theme (Material3) -->
    <style name="Theme.LearnHub" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryContainer">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>

    <!--
      Splash screen theme (Android 12+ SplashScreen API, backported via core-splashscreen).
      This is set as the initial theme in AndroidManifest and replaced after splash.
    -->
    <style name="Theme.LearnHub.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.LearnHub</item>
        
        <!-- Native Android 12+ attributes -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/background_dark</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="31">500</item>
    </style>

</resources>
