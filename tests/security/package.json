{
  "name": "learnhub-security-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Adversarial Firestore security-rules tests. Self-contained: install here, not in the site root.",
  "scripts": {
    "test": "node firestore-rules.test.mjs ../../firestore.rules 'LearnHub firestore.rules'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
